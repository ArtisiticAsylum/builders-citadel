# Snippets

Reusable fragments worth keeping — a config that took a day to get right, a query pattern, a deploy script. One file per snippet, a `#` title line saying what it's for. Never secrets, never client-owned code.
