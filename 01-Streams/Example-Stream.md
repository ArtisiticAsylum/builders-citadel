# Example Stream

## How to Treat This Stream
*(Every stream note starts with this section — the AI reads and obeys it before touching anything else in the stream.)*
Active. Normal reading rules. Docs live in `_Example-Stream-Docs/` (restricted — inventory below, contents on ask).

## Current State
*(2–5 lines, updated after every session that works on this stream.)*
Freshly created from the template. Nothing decided yet.

## Next Actions
- [ ] Replace this file with your first real stream.

## Docs Inventory (names only, never contents)
| File | One-liner |
|---|---|
