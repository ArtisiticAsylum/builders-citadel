# 01-Streams — cover page

One note per active project or life-area ("stream"). Each stream note is a thin **manager file**: current state, next actions, and a names-only inventory of its docs folder. Details live in the stream's restricted `_<Stream>-Docs/` folder beside it.

This cover page is not a stream. The live roster of streams belongs in your Meta-Map (see CLAUDE.md).
